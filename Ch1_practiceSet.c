#include <stdio.h>

int main(){

    // Write a C program to calculate area of rectangle takin inputs from the user.
        // int length = 3;
        // int breadth = 6;
        
      /*  int length , breadth;
        printf ("Enter length\n");
        scanf("%d", &length);

        printf("Enter breath\n");
        scanf ("%d", &breadth);

        printf ("The area of this rectangle is %d", length*breadth);
*/

// Problem 2: Calculate the area of circle and modify it to get to getvo;ume of cylinder given its radius and height.
/*
int r = 6;
int height = 10;
printf("The area of circle with radius %d is %f\n\n", r, 3.14*r*r);
printf("The volume of cylinder with radius %d and height %d is %f", r, height,  3.14*r*r*height);
*/

// Problem 3: write a c program to convert celcius to fehrenheit.

/*
float c = 37.0, f;

    f = ((9.0/5.0)*c) + 32;
    printf("The value in Fahrenheit is %f", f);

    */


    //Problem 4: write a C program to calculate simple interest 

        float p = 34.1;
        int r = 8;
        int t = 5;
        printf("The value of simple interest is %f ", (p*r*t)/100);


        return 0;
    }
    